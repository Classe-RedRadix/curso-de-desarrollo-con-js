// Calcular la suma de todos los números de un array.
console.log([1, 3, 5, 7, 11, 13].reduce((acc, cur) => acc + cur, 0))

// Concatenar todas las strings de un array.
console.log(['a', 'be', 'ce', 'da', 'rio'].reduce((acc, cur) => acc + cur, ''))
