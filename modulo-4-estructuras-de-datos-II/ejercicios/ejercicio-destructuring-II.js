let a = 1;
let b = 2;

[a,b] = [b,a]

// Intercambia el valor de a y b, sin crear una tercera variable,
// usando destructurazión.
// No es necesario que destructures un objeto.

console.log(a, b); // 2 1
