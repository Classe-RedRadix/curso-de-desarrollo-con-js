const obj = { a: 1, b: { c: 2, d: 5, e: { f: 9, g: 6 } } };

// Crea una función traverse que aplica una función a todas
// las hojas de este tipo de objetos recursivos.
// Consideramos hojas cualquier valor que no sea un objeto.
// Por ejemplo, crea una copía de este objetos, donde multipliques
// por dos cada numero encontrado.

/**
 * Pistas:
 * 1. Otro buen sitio para usar la recursividad.
 */
